import React from "react";

const FacultyOverview: React.FC = () => {
	return (
		<section className="flex items-center justify-center min-h-screen">
			<h3>Hello from Faculty Overview</h3>
		</section>
	);
};

export default FacultyOverview;
